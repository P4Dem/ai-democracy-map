# AIxD Threat Map

## Project root

This is the git root. All source code lives in `./aixd-threatmap-master 2/`.

## Key files

- `aixd-threatmap-master 2/CLAUDE.md` — full architecture, design system, component map, conventions
- `aixd-threatmap-master 2/README.md` — setup, data model, deployment
- `project_context.md` — high-level project context

## Important

- Run all commands from `./aixd-threatmap-master 2/`
- The source code, config, and assets are under that directory
- No `package.json` or `astro.config.mjs` at root level
